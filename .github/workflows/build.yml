name: 🔨 Build LG V60 Kernel + SukiSU Ultra

on:
  workflow_dispatch:
    inputs:
      kernel_branch:
        description: 'Branch kernel LineageOS'
        required: true
        default: 'lineage-22.2'
        type: choice
        options:
          - lineage-23.2
          - lineage-23.0
          - lineage-22.2

# ══════════════════════════════════════════════════════════════
# Job-level env: definisikan sekali, tersedia di semua steps
# ══════════════════════════════════════════════════════════════
env:
  ARCH: arm64
  SUBARCH: arm64
  CC: clang
  LD: ld.lld
  CLANG_TRIPLE: aarch64-linux-gnu-
  CROSS_COMPILE: aarch64-linux-gnu-
  CROSS_COMPILE_COMPAT: arm-linux-gnueabi-

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120   # FIX: Cegah hang tak terbatas, hemat CI quota

    steps:

    # ══════════════════════════════════════════════════════════
    # STEP 1: Setup Environment
    # ══════════════════════════════════════════════════════════

    - name: 📥 Checkout repository (untuk apply_hooks.py)
      uses: actions/checkout@v4

    - name: 🗑️ Free up disk space
      run: |
        echo "Disk sebelum cleanup:"
        df -h / | tail -1

        sudo rm -rf \
          /usr/share/dotnet \
          /usr/local/lib/android \
          /opt/ghc \
          /usr/local/.ghcup \
          /usr/share/swift \
          /opt/hostedtoolcache/CodeQL \
          /usr/local/share/boost

        sudo apt-get clean
        sudo apt-get autoremove -y

        echo "Disk setelah cleanup:"
        df -h / | tail -1

    - name: 📦 Install build dependencies
      run: |
        sudo apt-get update -y
        sudo apt-get install -y \
          git bc bison flex libssl-dev libelf-dev \
          libncurses5-dev python3 python-is-python3 \
          zip unzip wget curl cpio kmod \
          gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi \
          binutils-aarch64-linux-gnu binutils-arm-linux-gnueabi

    - name: ⚙️ Install Clang 17
      run: |
        wget -q https://apt.llvm.org/llvm.sh -O llvm.sh
        chmod +x llvm.sh
        sudo ./llvm.sh 17 all

        for tool in clang clang++ ld.lld llvm-ar llvm-nm \
                    llvm-objcopy llvm-objdump llvm-readelf \
                    llvm-strip llvm-ranlib; do
          if [ -f "/usr/bin/${tool}-17" ]; then
            sudo ln -sf "/usr/bin/${tool}-17" "/usr/local/bin/${tool}"
          fi
        done

        echo "Clang version:"
        clang --version | head -3
        echo ""
        echo "LLD version:"
        ld.lld --version

    # ══════════════════════════════════════════════════════════
    # STEP 2: Clone Kernel Source
    # ══════════════════════════════════════════════════════════

    - name: 📥 Clone kernel source
      run: |
        git clone --depth=1 \
          https://github.com/LineageOS/android_kernel_lge_sm8250 \
          -b ${{ github.event.inputs.kernel_branch }} kernel

        cd kernel
        echo "Kernel version:"
        head -3 Makefile | grep -E 'VERSION|PATCHLEVEL|SUBLEVEL' | \
          awk '{print $3}' | tr '\n' '.' | sed 's/\.$//'
        echo ""

    # ══════════════════════════════════════════════════════════
    # STEP 3: Inject SukiSU Ultra (Manual Method)
    # FIX (Migrasi): Direktori clone diubah dari 'KernelSU/'
    # menjadi 'SukiSU/' agar namespace konsisten dengan SukiSU Ultra.
    # ══════════════════════════════════════════════════════════

    - name: 💉 Clone SukiSU Ultra source
      run: |
        cd kernel

        echo "▶ Cloning SukiSU Ultra repository..."
        # FIX (Migrasi): Clone ke direktori 'SukiSU/' bukan 'KernelSU/'
        if ! git clone --depth=1 \
          https://github.com/SukiSU-Ultra/SukiSU-Ultra SukiSU; then
          echo "❌ Gagal clone SukiSU Ultra repository!"
          echo "   URL: https://github.com/SukiSU-Ultra/SukiSU-Ultra"
          exit 1
        fi

        echo "▶ Creating symlink (relative path)..."
        cd drivers
        # FIX (Migrasi): Path diupdate dari '../KernelSU/kernel'
        # menjadi '../SukiSU/kernel' sesuai nama direktori baru
        ln -sf ../SukiSU/kernel kernelsu
        cd ..

        # Verifikasi symlink
        if [ ! -L drivers/kernelsu ]; then
          echo "❌ Symlink gagal dibuat!"
          exit 1
        fi

        # Verifikasi target symlink dapat diakses (bukan dangling)
        if [ ! -d drivers/kernelsu ]; then
          echo "❌ Symlink ada tapi target tidak ditemukan (dangling symlink)!"
          echo "   Symlink points to: $(readlink drivers/kernelsu)"
          echo "   Resolved path:     $(readlink -f drivers/kernelsu)"
          exit 1
        fi

        echo "✅ Symlink: drivers/kernelsu -> $(readlink drivers/kernelsu)"
        ls -la drivers/kernelsu

    - name: 🔧 Integrate SukiSU into build system
      run: |
        cd kernel

        echo "▶ Patching drivers/Makefile..."
        if ! grep -qx 'obj-y += kernelsu/' drivers/Makefile; then
          echo 'obj-y += kernelsu/' >> drivers/Makefile
          echo "  ✅ Added kernelsu to Makefile"
        else
          echo "  ⏭️  kernelsu sudah ada di Makefile"
        fi

        echo "▶ Patching drivers/Kconfig..."
        if ! grep -q 'source "drivers/kernelsu/Kconfig"' drivers/Kconfig; then
          # FIX (Bad Practice): Gunakan sed dengan fallback echo untuk robustness
          # Coba insert sebelum 'endmenu', jika gagal append ke akhir file
          if grep -q '^endmenu' drivers/Kconfig; then
            sed -i '/^endmenu/i source "drivers/kernelsu/Kconfig"' drivers/Kconfig
            echo "  ✅ Added kernelsu to Kconfig (via sed before endmenu)"
          else
            echo 'source "drivers/kernelsu/Kconfig"' >> drivers/Kconfig
            echo "  ✅ Added kernelsu to Kconfig (appended to end)"
          fi
        else
          echo "  ⏭️  kernelsu sudah ada di Kconfig"
        fi

        echo ""
        echo "Verifikasi integrasi:"
        echo "  Makefile: $(grep 'kernelsu' drivers/Makefile)"
        echo "  Kconfig:  $(grep 'kernelsu' drivers/Kconfig)"

    # ══════════════════════════════════════════════════════════
    # STEP 4: Apply Manual Hooks
    # FIX (Bug): Gunakan path absolut $GITHUB_WORKSPACE agar
    # apply_hooks.py dan kernel dir ditemukan dengan benar
    # ══════════════════════════════════════════════════════════

    - name: 🔧 Apply kernel hooks
      run: |
        echo "▶ Running apply_hooks.py..."
        echo ""

        # FIX (Bug): Gunakan path absolut — scripts/ ada di repo root,
        # bukan di dalam direktori kernel
        SCRIPT="$GITHUB_WORKSPACE/scripts/apply_hooks.py"
        KERNEL_DIR="$GITHUB_WORKSPACE/kernel"

        if [ ! -f "$SCRIPT" ]; then
          echo "❌ apply_hooks.py tidak ditemukan di: $SCRIPT"
          exit 1
        fi

        if ! python3 "$SCRIPT" "$KERNEL_DIR" 2>&1 | tee patch.log; then
          echo ""
          echo "╔═══════════════════════════════╗"
          echo "║  ❌ PATCH SCRIPT GAGAL        ║"
          echo "╚═══════════════════════════════╝"
          cat patch.log
          exit 1
        fi

        echo ""
        echo "▶ Validating critical patches..."

        CRITICAL_FILES=(
          "fs/exec.c:ksu_execveat_hook"
          "fs/open.c:ksu_handle_faccessat"
          "drivers/input/input.c:ksu_input_hook"
        )

        FAILED=0
        for entry in "${CRITICAL_FILES[@]}"; do
          FILE="${entry%%:*}"
          MARKER="${entry##*:}"

          if grep -q "$MARKER" "$KERNEL_DIR/$FILE" 2>/dev/null; then
            echo "  ✅ $FILE ($MARKER)"
          else
            echo "  ❌ CRITICAL: $FILE tidak memiliki $MARKER"
            FAILED=1
          fi
        done

        if [ $FAILED -eq 1 ]; then
          echo ""
          echo "❌ Patch kritis gagal! Build akan error."
          exit 1
        fi

        echo ""
        echo "✅ Semua patch kritis berhasil diaplikasikan"

    # ══════════════════════════════════════════════════════════
    # STEP 5: Configure Kernel
    # FIX: Semua variabel build sudah di-set via job-level env:
    # Tidak perlu re-export, hanya referensi langsung
    # ══════════════════════════════════════════════════════════

    - name: 🏗️ Configure kernel
      run: |
        cd kernel

        echo "════════════════════════════════════════"
        echo "  [1/3] Base defconfig"
        echo "════════════════════════════════════════"

        make O=out ARCH=arm64 \
          CC=clang \
          CLANG_TRIPLE=aarch64-linux-gnu- \
          CROSS_COMPILE=aarch64-linux-gnu- \
          CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
          vendor/kona-perf_defconfig

        echo ""
        echo "════════════════════════════════════════"
        echo "  [2/3] Merge device-specific config"
        echo "════════════════════════════════════════"

        TIMELM_CFG=""
        for cfg_path in \
          "arch/arm64/configs/vendor/timelm.config" \
          "arch/arm64/configs/timelm.config"; do
          if [ -f "$cfg_path" ]; then
            TIMELM_CFG="$cfg_path"
            break
          fi
        done

        if [ -n "$TIMELM_CFG" ]; then
          echo "  Using: $TIMELM_CFG"
          # FIX (Bug): Hilangkan flag -m agar merge_config.sh
          # menulis hasil merge ke out/.config secara lengkap
          KCONFIG_CONFIG=out/.config ./scripts/kconfig/merge_config.sh \
            out/.config \
            "$TIMELM_CFG"
        else
          echo "  ⚠️  timelm.config tidak ditemukan, skip merge"
          echo "     Build akan tetap jalan dengan kona-perf_defconfig"
        fi

        echo ""
        echo "════════════════════════════════════════"
        echo "  [3/3] Enable SukiSU Ultra configs"
        echo "════════════════════════════════════════"

        # FIX (Migrasi): Aktifkan config SukiSU Ultra secara lengkap
        ./scripts/config --file out/.config \
          --enable CONFIG_KSU \
          --enable CONFIG_OVERLAY_FS

        make O=out ARCH=arm64 \
          CC=clang \
          CLANG_TRIPLE=aarch64-linux-gnu- \
          CROSS_COMPILE=aarch64-linux-gnu- \
          CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
          olddefconfig

        echo ""
        echo "▶ Verifying SukiSU Ultra configs..."

        # FIX (Migrasi): Cek CONFIG_KSU sebagai primary SukiSU Ultra config
        if grep -qE '^CONFIG_KSU=y' out/.config; then
          echo "  ✅ CONFIG_KSU=y (SukiSU Ultra AKTIF)"
        else
          echo "  ❌ CONFIG_KSU tidak aktif!"
          echo ""
          echo "Debugging info:"
          echo "1. Kconfig integration:"
          grep 'kernelsu' drivers/Kconfig || echo "   TIDAK ADA!"
          echo ""
          echo "2. SukiSU Kconfig file:"
          ls -la drivers/kernelsu/Kconfig || echo "   FILE TIDAK ADA!"
          echo ""
          echo "3. Config dependency check:"
          grep -A10 'config KSU' drivers/kernelsu/Kconfig || echo "   TIDAK BISA DIBACA!"
          echo ""
          echo "4. Current .config status:"
          grep -i 'ksu\|kernelsu\|sukisu' out/.config || echo "   TIDAK ADA MENTION!"
          exit 1
        fi

        echo ""
        echo "Other important configs:"
        grep '^CONFIG_MODULES=' out/.config || echo "  CONFIG_MODULES not set"
        grep '^CONFIG_OVERLAY_FS=' out/.config || echo "  CONFIG_OVERLAY_FS not set"

    # ══════════════════════════════════════════════════════════
    # STEP 6: Build Kernel
    # ══════════════════════════════════════════════════════════

    - name: 🔨 Compile kernel
      run: |
        cd kernel

        # FIX (Bad Practice): set -euo pipefail untuk error handling global
        # PIPESTATUS digunakan untuk logging tanpa bergantung pada set -e saja
        set -euo pipefail

        BUILD_START=$(date +%s)

        echo "════════════════════════════════════════"
        echo "  Starting kernel compilation (SukiSU Ultra)"
        echo "════════════════════════════════════════"
        echo "Compiler: $(clang --version | head -1)"
        echo "Cores:    $(nproc)"
        echo "Disk free: $(df -h / | tail -1 | awk '{print $4}')"
        echo "Time:     $(date '+%Y-%m-%d %H:%M:%S')"
        echo "════════════════════════════════════════"
        echo ""

        # Gunakan timeout agar tidak hang tak terbatas
        timeout 90m make O=out ARCH=arm64 \
          CC=clang \
          CLANG_TRIPLE=aarch64-linux-gnu- \
          CROSS_COMPILE=aarch64-linux-gnu- \
          CROSS_COMPILE_COMPAT=arm-linux-gnueabi- \
          -j$(nproc) \
          2>&1 | tee build.log || true

        BUILD_EXIT=${PIPESTATUS[0]:-1}
        BUILD_END=$(date +%s)
        BUILD_TIME=$(( BUILD_END - BUILD_START ))

        echo ""
        echo "════════════════════════════════════════"
        echo "  Build finished"
        echo "════════════════════════════════════════"
        echo "Exit code: $BUILD_EXIT"
        echo "Duration:  $(printf '%02d:%02d:%02d' $((BUILD_TIME/3600)) $((BUILD_TIME%3600/60)) $((BUILD_TIME%60)))"
        echo "Disk free: $(df -h / | tail -1 | awk '{print $4}')"
        echo "════════════════════════════════════════"
        echo ""

        if [ "$BUILD_EXIT" -ne 0 ]; then
          echo "╔════════════════════════════════════╗"
          echo "║   ❌ COMPILATION FAILED            ║"
          echo "╚════════════════════════════════════╝"
          echo ""

          AVAIL_KB=$(df / | tail -1 | awk '{print $4}')
          AVAIL_MB=$(( AVAIL_KB / 1024 ))

          if [ "$AVAIL_MB" -lt 500 ]; then
            echo "🚨 DISK SPACE KRITIS: ${AVAIL_MB}MB tersisa"
            echo "   Build mungkin gagal karena disk penuh"
            echo ""
          fi

          if [ -f build.log ] && [ -s build.log ]; then
            echo "▶ First error in build.log:"
            echo "─────────────────────────────────────"

            # FIX (Bad Practice): Regex lebih presisi untuk C compiler
            # dan make errors, hindari false positive dari -Werror warnings
            ERRLINE=$(grep -n \
              -E '^[^:]+\.(c|h|S):[0-9]+:[0-9]+: error:|make\[[0-9]+\]: \*\*\*|No space left|ld\.lld: error' \
              build.log | head -1 | cut -d: -f1)

            if [ -n "$ERRLINE" ]; then
              START=$(( ERRLINE > 15 ? ERRLINE - 15 : 1 ))
              END=$(( ERRLINE + 30 ))
              sed -n "${START},${END}p" build.log
            else
              echo "No explicit error found in log"
              echo "Showing last 50 lines:"
              tail -50 build.log
            fi
          else
            echo "❌ build.log tidak ada atau kosong!"
          fi

          exit 1
        fi

        IMAGES_FOUND=0
        echo "▶ Checking build artifacts:"
        for img in Image.gz-dtb Image.gz Image; do
          if [ -f "out/arch/arm64/boot/$img" ]; then
            SIZE=$(du -h "out/arch/arm64/boot/$img" | cut -f1)
            echo "  ✅ $img ($SIZE)"
            IMAGES_FOUND=1
          fi
        done

        if [ "$IMAGES_FOUND" -eq 0 ]; then
          echo ""
          echo "❌ Tidak ada kernel image yang dihasilkan!"
          ls -la out/arch/arm64/boot/ || echo "  Directory tidak ada!"
          exit 1
        fi

        echo ""
        echo "╔════════════════════════════════════╗"
        echo "║   ✅ BUILD SUCCESS (SukiSU Ultra)  ║"
        echo "╚════════════════════════════════════╝"

    # ══════════════════════════════════════════════════════════
    # STEP 7: Create Flashable ZIP
    # FIX (Migrasi): Device name list diperlengkap untuk LG V60
    # FIX (Bug): DTB copy menggunakan check eksplisit sebelum find
    # ══════════════════════════════════════════════════════════

    - name: 📦 Create AnyKernel3 flashable ZIP
      run: |
        echo "▶ Cloning AnyKernel3..."
        git clone --depth=1 https://github.com/osm0sis/AnyKernel3 AnyKernel3

        cd AnyKernel3

        echo "▶ Configuring for LG V60 ThinQ (timelm)..."

        # FIX (Migrasi/Security): Lengkapi device names LG V60
        # agar device check tidak gagal di unit dengan prop yang berbeda
        sed -i 's|do\.devicecheck=.*|do.devicecheck=1|' anykernel.sh
        sed -i 's|device\.name1=.*|device.name1=timelm|' anykernel.sh
        sed -i 's|device\.name2=.*|device.name2=lge_v60|' anykernel.sh
        sed -i 's|device\.name3=.*|device.name3=LM-V600|' anykernel.sh
        sed -i 's|device\.name4=.*|device.name4=LM-V600N|' anykernel.sh
        sed -i 's|device\.name5=.*|device.name5=|' anykernel.sh
        sed -i 's|block=.*|block=/dev/block/bootdevice/by-name/boot;|' anykernel.sh
        sed -i 's|is_slot_device=.*|is_slot_device=1;|' anykernel.sh

        BRANCH="${{ github.event.inputs.kernel_branch }}"
        KERNEL_STR="SukiSU Ultra for LG V60 ThinQ (${BRANCH})"
        sed -i "s|kernel\.string=.*|kernel.string=${KERNEL_STR}|" anykernel.sh

        echo "▶ Copying kernel image..."
        BOOT="../kernel/out/arch/arm64/boot"
        COPIED=0

        for img in Image.gz-dtb Image.gz Image; do
          if [ -f "$BOOT/$img" ]; then
            cp "$BOOT/$img" ./
            SIZE=$(du -h "$img" | cut -f1)
            echo "  ✅ $img copied ($SIZE)"
            COPIED=1
            break
          fi
        done

        if [ "$COPIED" -eq 0 ]; then
          echo "  ❌ No kernel image found!"
          exit 1
        fi

        echo "▶ Copying DTBs (if any)..."
        mkdir -p dtbs
        # FIX (Bug): Check eksplisit direktori DTS sebelum find
        # mencegah silent error jika direktori tidak ada
        if [ -d "$BOOT/dts" ]; then
          DTB_COUNT=$(find "$BOOT/dts" -name "*.dtb" | wc -l)
          if [ "$DTB_COUNT" -gt 0 ]; then
            find "$BOOT/dts" -name "*.dtb" -exec cp {} dtbs/ \;
            echo "  ✅ $DTB_COUNT DTB files copied"
          else
            echo "  ℹ️  Direktori DTS ada tapi tidak ada file .dtb"
          fi
        else
          echo "  ℹ️  No DTS directory (DTBs mungkin bundled dalam Image)"
        fi

        echo "▶ Creating flashable ZIP..."
        BUILD_DATE=$(date +%Y%m%d-%H%M)
        ZIP_NAME="SukiSU-Ultra-V60-${BRANCH}-${BUILD_DATE}.zip"

        zip -r9 "../${ZIP_NAME}" . \
          -x ".git/*" \
          -x "README.md" \
          -x "*.placeholder" \
          -x ".gitignore"

        cd ..

        ZIP_SIZE=$(du -h "${ZIP_NAME}" | cut -f1)
        echo ""
        echo "╔════════════════════════════════════╗"
        echo "║   ✅ ZIP CREATED                   ║"
        echo "╚════════════════════════════════════╝"
        echo "File: ${ZIP_NAME}"
        echo "Size: ${ZIP_SIZE}"
        echo ""
        echo "▶ ZIP contents:"
        unzip -l "${ZIP_NAME}" | grep -E 'Image|\.dtb|anykernel\.sh'

    # ══════════════════════════════════════════════════════════
    # STEP 8: Upload Artifacts
    # FIX (Migrasi): Konsolidasi log artifacts untuk kemudahan debug
    # ══════════════════════════════════════════════════════════

    - name: ⬆️ Upload kernel ZIP
      uses: actions/upload-artifact@v4
      with:
        name: SukiSU-Ultra-V60-Kernel-${{ github.event.inputs.kernel_branch }}
        path: SukiSU-Ultra-V60-*.zip
        retention-days: 7
        compression-level: 0

    # FIX (Migrasi/Bad Practice): Konsolidasikan semua logs ke satu artifact
    - name: ⬆️ Upload build logs & config
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-logs-${{ github.event.inputs.kernel_branch }}
        path: |
          kernel/build.log
          patch.log
          kernel/out/.config
        retention-days: 7

    # ══════════════════════════════════════════════════════════
    # STEP 9: Summary
    # FIX (Migrasi — Kritis): URL Manager diupdate ke SukiSU Ultra
    # ══════════════════════════════════════════════════════════

    - name: 📊 Build summary
      if: always()
      run: |
        echo ""
        echo "════════════════════════════════════════════════════════"
        echo "               BUILD SUMMARY — SukiSU Ultra"
        echo "════════════════════════════════════════════════════════"
        echo ""

        if ls SukiSU-Ultra-V60-*.zip 1>/dev/null 2>&1; then
          echo "✅ Status: SUCCESS"
          echo ""
          echo "📦 Output files:"
          ls -lh SukiSU-Ultra-V60-*.zip
          echo ""
          echo "📥 Download Instructions:"
          echo "   1. Klik tab 'Summary' di bagian atas"
          echo "   2. Scroll ke bawah ke section 'Artifacts'"
          echo "   3. Download 'SukiSU-Ultra-V60-Kernel-...'"
          echo ""
          echo "📱 Installation (via TWRP/OrangeFox):"
          echo "   1. Reboot ke recovery mode"
          echo "   2. Install → Pilih file ZIP yang sudah didownload"
          echo "   3. Swipe to confirm flash"
          echo "   4. Reboot system"
          echo ""
          # FIX (Migrasi — KRITIS): URL diupdate ke SukiSU Ultra Manager
          # URL lama (tiann/KernelSU) adalah upstream KernelSU, BUKAN SukiSU Ultra
          echo "   5. Install SukiSU Ultra Manager APK:"
          echo "      https://github.com/SukiSU-Ultra/SukiSU-Ultra/releases"
          echo ""
          echo "⚠️  IMPORTANT:"
          echo "   - Pastikan backup boot.img sebelum flash!"
          echo "   - Jika bootloop, restore boot.img dari recovery"
          echo "   - Safe mode: Tahan VOL- saat booting untuk disable root"
          echo "   - JANGAN gunakan KernelSU Manager — gunakan SukiSU Ultra Manager"
        else
          echo "❌ Status: FAILED"
          echo ""
          echo "📋 Troubleshooting:"
          echo "   1. Download 'build-logs' dari Artifacts (berisi build.log, patch.log, .config)"
          echo "   2. Cari baris yang mengandung 'error:' di build.log"
          echo "   3. Cek patch.log untuk proses patching SukiSU Ultra"
          echo ""
          echo "🔧 Common Issues:"
          echo "   - Disk full: Hapus cache atau pilih runner dengan lebih banyak space"
          echo "   - Config error: Cek apakah timelm.config kompatibel dengan branch"
          echo "   - Patch error: Cek apakah kernel version cocok dengan SukiSU Ultra"
          echo "   - Symlink error: Pastikan SukiSU Ultra repo berhasil di-clone"
          echo ""
          echo "💬 Need help? Open an issue di repository ini"
        fi

        echo ""
        echo "════════════════════════════════════════════════════════"
        echo "Branch:   ${{ github.event.inputs.kernel_branch }}"
        echo "Build time: $(date '+%Y-%m-%d %H:%M:%S %Z')"
        echo "Run URL:  ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        echo "════════════════════════════════════════════════════════"
